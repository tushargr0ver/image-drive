# ---------- Client Environment (.env.example for Vite) ----------
# Copy to .env.local or .env and adjust as needed.
# Variables must be prefixed with VITE_ to be exposed to the client.

# Backend API base URL. Leave blank to rely on relative /api calls + Vercel rewrites.
VITE_API_URL=https://your-backend-domain.onrender.com

# Feature flags
VITE_FEATURE_UPLOAD_DRAG_DROP=true
VITE_FEATURE_EXPERIMENTAL_UI=false

# Environment label (optional)
VITE_APP_ENV=development
